


<%# Be careful about the naming, it starts with an underscore to indicate that its a partial file, it will be imported to other html files. %>

<%# its all libraries, we didnt go through it... %>

<%# This form is the key of the 'edit' and 'new' pages, it will add every thing from the database. %>

<%= form_for @artist do |f| %>
  <div class="input-field col s6">
      <%= f.label :name, 'Name'%>
      <%= f.text_field :name%>
  </div>
  <div class="input-field col s6">
      <%= f.label :albums, 'Albums' %>
      <%= f.text_field :albums %>
  </div>
  <div class="input-field col s6">
      <%= f.label :hometown, 'Hometown' %>
      <%= f.text_field :hometown %>
  </div>
  <div class="input-field col s6">
      <%= f.label :img, 'Image' %>
      <%= f.text_field :img %>
  </div>
  <%= f.submit "Submit", :class => "btn waves-effect waves-light" %>
<% end %>