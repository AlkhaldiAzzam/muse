<%# ALl of this is in the lesson %>

<h1>All the Artists</h1>


<%= link_to "Add an Artist", new_artist_path, { :class => "button waves-effect waves-light btn" } %>
<br><br>
<%# This will add the 'add artist' button %>

<table class="striped bordered">
  <tr>
    <th>Artist</th>
    <th>Image</th>
    <th>Albums</th>
    <th>Hometown</th>
    <th>Edit</th>
    <th>Delete</th>


  </tr>

  <% @artists.each do |artist| %>
  <%# '@artists' is in the controller %>
      <tr>
        <td><%= link_to artist.name, artist_path(artist) %></td> <%#'link_to' is instead of the 'a' tag. and 'artist_path(artist)' will find the artist, its only the '_path' keyword %>
        <td><%= image_tag(artist.img, size: "100x100", alt: "Artist Image") %></td> <%# it will get the image and add some styles to it%>
        <td><%= artist.albums %></td>
        <td><%= artist.hometown %></td>

         <td><a class="btn-floating btn-large red" href="<%= edit_artist_path(artist) %>" > <%#This will add the edit button..%>
           <em class="large material-icons">Edit Artist</em>
          </a>
        </td>
          <%#This will add the delete button%>
           <td><a class="btn-floating btn-large red" data-confirm="Are you sure you want to delete <%= artist.name %>?" data-method="delete" href="/artists/<%= artist.id %>" rel="nofollow"><i class="large material-icons">Delete Artist</i>
           </a> 
        </td>
      </tr>
  <% end %>
</table>